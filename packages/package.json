{
  "name": "script-utils",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev:bgm": "cd scripts/bangumi && vite",
    "build:bgm": "cd scripts/bangumi && pnpm build",
    "build": "tsc && vite build",
    "preview": "vite preview"
  }
}
